### CE-01 | `odeme_tarihi` yokken `vade_tarihi` ödeme gibi sayılıyor
- DOSYA:SATIR = fatura_api.py:6999
- SEVIYE = P0 (para/adet yanlis)
- NE OLUYOR = `vadeli_alimlar` içindeki `durum='odendi'` satırlarında gerçek ödeme tarihi yoksa kod `vade_tarihi`ni ödeme tarihi yerine kullanıyor. Bu satırlar hem `odeme_adaylari`na giriyor hem de `hesaplanan_acik` ve `yuruyen_bakiye` hesabında borcu düşüyor.
- SENARYO = `vadeli_alimlar` kaydı: `durum='odendi'`, `odeme_tarihi=NULL`, `vade_tarihi='2026-08-15'`, `tutar=1000` -> sorgu bu kaydı `2026-08-15` tarihli ödeme diye alır -> `odeme_toplam` 1000 artar -> `hesaplanan_acik` 1000 eksik görünür.
- KANIT = `COALESCE(odeme_tarihi, vade_tarihi)::text AS tarih` ve `WHERE durum='odendi' AND COALESCE(odeme_tarihi, vade_tarihi) >= %s::date`
- ONERI = `durum='odendi'` için `odeme_tarihi` zorunlu olsun; boşsa satırı ödeme hesabına hiç sokma.

### CE-02 | Geniş isim eşleşmesi başka tedarikçinin faturasını aynı ekstreye katıyor
- DOSYA:SATIR = fatura_api.py:6967
- SEVIYE = P0 (para/adet yanlis)
- NE OLUYOR = Fatura seçimi, kanonik/eşdeğer ad dışında ham substring ve token-kapsama ile yapılıyor. Tek tedarikçi ekstresi, ortak bir kelime ya da 3+ karakterlik parça yüzünden başka firmanın faturasını da alabiliyor; aynı blokta `.upper()` / `.lower()` ham kullanıldığı için Türkçe `I/İ/ı/i` varyantları da güvenli normalize edilmiyor.
- SENARYO = `tedarikci=KAHVE` ile çağrı yapılır -> `ara.lower() in ad.lower()` veya `_ara_tok <= ft` koşulu `... KAHVE ...` geçen birden çok farklı firmayı seçer -> `faturalar` büyür -> `fatura_toplam_6ay`, `hesaplanan_acik` ve `yuruyen_bakiye` başka firmanın borcunu da içerir.
- KANIT = `if ad.strip().upper() in _es_upper`, `if ara.lower() in ad.lower():`, `if _ara_tok and ft and (_ara_tok <= ft or ft <= _ara_tok):`
- ONERI = İsimle aramada yalnız tek normalize fonksiyonla tam/kanıtlı eşleşme kullan; serbest substring ve tek-token kapsama ile bakiyeye veri alma.

### CE-03 | Damgasız ödeme adayları açıklama metniyle bu cariye yazılıyor
- DOSYA:SATIR = fatura_api.py:7044
- SEVIYE = P0 (para/adet yanlis)
- NE OLUYOR = `damga` yoksa ödeme satırı sadece `aciklama` metni üzerinden `_es_es(...)` ile tedarikçiye bağlanıyor. Aynı yaklaşım `bekleyen_vadeler` süzmesinde de `tedarikci + aciklama` serbest metnine uygulanıyor; not alanında geçen bir ad, başka firmanın hareketini bu ekstreye sokabiliyor.
- SENARYO = Kart hareketi `damga=''`, `aciklama='ATALAY KAHVE avans kapama'`, `tutar=5000` ama gerçek karşı taraf başka biri -> `_odeme_dahil` bunu bu tedarikçiye dahil eder -> `odeme_toplam` 5000 artar -> açık bakiye 5000 düşük görünür.
- KANIT = `return _es_es(r.get("aciklama"))` ve `if _es_es(f"{r['ted']} {r['aciklama'] or ''}")`
- ONERI = Damgasız satırları bakiyeye dahil etme; açıklama eşleşmesini yalnız öneri/inceleme listesinde tut, mutabakat için kimlikli bağ şartı koy.

### CE-04 | `hareketler` listesi 80 satırda sessiz kesiliyor
- DOSYA:SATIR = fatura_api.py:7159
- SEVIYE = P1 (veri bozulur/iz kaybolur)
- NE OLUYOR = Yürüyen bakiye tam hareket setiyle hesaplandıktan sonra yanıta yalnız son 80 satır konuyor. Yanıt, listenin kesildiğini söyleyen bir bayrak ya da toplam hareket sayısı vermediği için kullanıcı eski hareketleri hiç yok sanabiliyor.
- SENARYO = Bir tedarikçide 130 hareket var -> API `hareketler[-80:]` döner -> ilk 50 hareket görünmez ama görünen ilk satırın `bakiye`si onlar işlenmiş gibi başlar -> denetçi listeyi tam ekstre sanarsa iz kaybı oluşur.
- KANIT = `"hareketler": hareketler[-80:],` ve `"yuruyen_bakiye": (hareketler[-1]["bakiye"] if hareketler else 0.0)`
- ONERI = `hareketler` için sayfalama kullan veya en azından `hareketler_kisaltildi` ve toplam hareket sayısını açıkça döndür.

### CE-05 | GRNI sorgu hatası yutulup ekstre başarıyla dönüyor
- DOSYA:SATIR = fatura_api.py:6896
- SEVIYE = P2 (kozmetik/dayaniklilik)
- NE OLUYOR = `belge_talep` sorgusu patlarsa fonksiyon hatayı dışarı vermiyor; sıfır adet/sıfır tutar dönüp hata metnini alt alana gömüyor. `/cari-ekstre` yanıtı böylece başarılı görünürken `faturasiz_teslimat` özeti sessizce eksik kalıyor.
- SENARYO = `belge_talep` sorgusunda kolon/izin hatası oluşur -> `except` çalışır -> `{"adet":0,"toplam_tl":0.0,"hata":"..."}` döner -> üst API normal cevap verir, kullanıcı GRNI yok sanır.
- KANIT = `except Exception as e: ... return {"adet": 0, "toplam_tl": 0.0, "hata": str(e)[:120]}`
- ONERI = Bu sorgu başarısızsa üst seviyede hata üret; sıfır özetle başarılı cevap verme.
